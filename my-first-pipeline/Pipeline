pipeline {
  agent {
    docker { image 'node:16-alpine' }
  }
  stages {
    stage('Checkout') {
      steps {
        checkout scm
      }
    }
    stage('Test') {
      steps {
        sh 'node --version'
      }
    }
    // Comment out or skip Kubernetes until cluster is ready
    // stage('Run Pod') {
    //   steps {
    //     sh 'kubectl apply -f pod.yml'
    //   }
    // }
  }
}

